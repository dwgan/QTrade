# Use this file as a template, then export the value in your shell.
# Never commit a real token.
TUSHARE_TOKEN=replace-with-your-token
# Optional: leave empty to use the official Tushare API endpoint.
TUSHARE_API_URL=
